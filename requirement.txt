# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Excel file handling
openpyxl>=3.1.0

# Environment variables
python-dotenv>=1.0.0

# Excel automation (pywin32 for handling open files)
pywin32>=306

# Email (built-in, no installation needed)
# smtplib, email, ssl are part of Python standard library

# Logging (built-in, no installation needed)
# logging is part of Python standard library

# Live Dasboard
streamlit
plotly

# Optional: For better date parsing
# pytz>=2023.3
